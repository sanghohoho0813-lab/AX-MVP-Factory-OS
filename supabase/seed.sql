-- =====================================================================
-- Stage 12A · seed
-- ---------------------------------------------------------------------
-- 시드는 의도적으로 최소한만 둔다.
--   * 워크스페이스/도메인 데이터는 실제 사용자(auth.users)에 종속되므로
--     로그인·워크스페이스 생성 후 앱의 "로컬 데이터 가져오기"로 채운다.
--   * auth.users 는 Supabase Auth(가입) 또는 관리 API 로만 안전하게 생성한다.
--     여기서 임의 비밀번호 계정을 만들지 않는다(비밀값을 Git 에 남기지 않기 위함).
--
-- 로컬 개발에서 데모 계정을 원하면 아래 방법을 사용한다(수동, 커밋하지 않음):
--   1) supabase start
--   2) Studio(http://localhost:54323) → Authentication → Add user 로 가입
--   3) 앱에서 로그인 → 워크스페이스 생성 → 로컬 데이터 가져오기 실행
-- =====================================================================

-- (의도적으로 비어 있음)
